<?xml version="1.0" encoding="utf-8"?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CSimple.Components.CapturePreviewCard">
    <Border Style="{StaticResource ModernCardStyle}"
            FlexLayout.Basis="{OnIdiom Phone=100%, Default=48%}"
            FlexLayout.Grow="1"
            MinimumWidthRequest="300">
        <VerticalStackLayout Spacing="12">
            <Label Text="Capture Preview"
                   Style="{StaticResource SubHeaderStyle}" />
            <Grid RowDefinitions="Auto,Auto,Auto"
                  ColumnDefinitions="*,*"
                  Margin="0,8">
                <!-- Screen Preview -->
                <VerticalStackLayout Grid.Row="0"
                                     Grid.Column="0"
                                     Margin="0,0,6,4">
                    <Label Text="Screen Capture"
                           Style="{StaticResource CaptionStyle}" />
                    <Frame Style="{StaticResource PreviewFrameStyle}"
                           HeightRequest="160"
                           Margin="0,4,0,0">
                        <Grid>
                            <Image x:Name="ScreenCaptureImage"
                                   Source="{Binding ScreenCaptureSource, Mode=OneWay}"
                                   Aspect="AspectFill" />
                            <!-- Overlay and loading indicator for screen -->
                            <Grid BackgroundColor="#80000000"
                                  IsVisible="{Binding IsScreenCaptureInactive}">
                                <VerticalStackLayout HorizontalOptions="Center"
                                                     VerticalOptions="Center">
                                    <ActivityIndicator IsRunning="{Binding IsLoadingScreenPreview}"
                                                       Color="{StaticResource PrimaryColor}"
                                                       HeightRequest="40"
                                                       WidthRequest="40" />
                                    <Label x:Name="ScreenCaptureStatus"
                                           Text="Waiting for screen feed..."
                                           HorizontalOptions="Center"
                                           TextColor="White"
                                           FontSize="12"
                                           Margin="0,8,0,0" />
                                </VerticalStackLayout>
                            </Grid>
                        </Grid>
                    </Frame>
                </VerticalStackLayout>
                <!-- Webcam Preview -->
                <VerticalStackLayout Grid.Row="0"
                                     Grid.Column="1"
                                     Margin="6,0,0,4">
                    <Label Text="Webcam Feed"
                           Style="{StaticResource CaptionStyle}" />
                    <Frame Style="{StaticResource PreviewFrameStyle}"
                           HeightRequest="160"
                           Margin="0,4,0,0">
                        <Grid>
                            <Image x:Name="WebcamCaptureImage"
                                   Source="{Binding WebcamCaptureSource, Mode=OneWay}"
                                   Aspect="AspectFill" />
                            <!-- Overlay and loading indicator for webcam -->
                            <Grid BackgroundColor="#80000000"
                                  IsVisible="{Binding IsWebcamCaptureInactive}">
                                <VerticalStackLayout HorizontalOptions="Center"
                                                     VerticalOptions="Center">
                                    <ActivityIndicator IsRunning="{Binding IsLoadingWebcamPreview}"
                                                       Color="{StaticResource PrimaryColor}"
                                                       HeightRequest="40"
                                                       WidthRequest="40" />
                                    <Label x:Name="WebcamCaptureStatus"
                                           Text="Waiting for webcam feed..."
                                           HorizontalOptions="Center"
                                           TextColor="White"
                                           FontSize="12"
                                           Margin="0,8,0,0" />
                                </VerticalStackLayout>
                            </Grid>
                        </Grid>
                    </Frame>
                </VerticalStackLayout>
                <!-- Input Status -->
                <Frame Grid.Row="1"
                       Grid.Column="0"
                       Grid.ColumnSpan="2"
                       Margin="0,12,0,0"
                       Style="{StaticResource PreviewFrameStyle}"
                       Padding="12">
                    <VerticalStackLayout>
                        <Label Text="Input Activity"
                               Style="{StaticResource CaptionStyle}"
                               Margin="0,0,0,4" />
                        <Label x:Name="ButtonLabel"
                               Text="No active key/mouse presses"
                               MaxLines="4"
                               LineBreakMode="TailTruncation"
                               Style="{StaticResource BodyTextStyle}" />
                    </VerticalStackLayout>
                </Frame>
            </Grid>
        </VerticalStackLayout>
    </Border>
</ContentView>